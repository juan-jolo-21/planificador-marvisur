export interface Person {
    Nombre?: string,
    APP ?: string,
    APM ?: string,
    dni ?: number

    // public getCompleteName() :string {
    //     return `${this.Nombre} ${this.APP} ${this.APM} ${this.dni}`;
    // }
}
